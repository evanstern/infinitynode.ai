## Slack agent DM fix
- Discovered Slack DMs were failing because `nativeStreaming`/`streaming` were enabled without required scopes, causing missing_scope errors and dropping inbound events.
- Disabled `nativeStreaming` and set `streaming` to `off` in `openclaw.json`; Slack DMs now work again.
