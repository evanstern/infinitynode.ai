---
title: MDX field note
description: A short MDX post proving that richer writing can live beside Markdown.
pubDate: 2026-05-25
draft: false
---

export const Note = ({ children }) => <p class="inline-note">{children}</p>;

MDX is available for posts that need a little structure without turning the blog into an app.

<Note>Use this sparingly. Most posts can stay as plain Markdown.</Note>

The rule of thumb: write first, add components only when they clarify the idea.
